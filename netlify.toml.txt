[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application routing rules to handle routing transitions seamlessly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
